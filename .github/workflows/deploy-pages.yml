name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: https://bachhhhhhhhhhhhhh.github.io/livewweb/
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build static site
        run: npm run build:pages
        env:
          GITHUB_PAGES: '1'
          VITE_STATIC_MIRROR: '1'
          VITE_UNLOCK_ALL: '1'
          VITE_RSS_DIRECT_TO_RELAY: '1'
          VITE_WS_API_URL: ${{ vars.LIVEWWEB_API_URL || 'https://livewweb.vercel.app' }}
          PROXY_TO_UPSTREAM_API: '1'

      - name: Prepare GitHub Pages entrypoints
        run: |
          touch dist/.nojekyll
          printf '%s\n' \
            '<!DOCTYPE html>' \
            '<html lang="en">' \
            '<head>' \
            '  <meta charset="UTF-8" />' \
            '  <meta http-equiv="refresh" content="0;url=./dashboard.html" />' \
            '  <script>location.replace("./dashboard.html");</script>' \
            '  <title>World Monitor</title>' \
            '</head>' \
            '<body></body>' \
            '</html>' \
            > dist/index.html
          cp dist/index.html dist/404.html

      - name: Verify build artifacts
        run: |
          test -f dist/dashboard.html
          test -f dist/live-seed/bootstrap-fast.json
          test -f dist/live-seed/bootstrap-slow.json
          test -f dist/live-seed/feed-digest-full-en.json
          node <<'NODE'
          import { readFileSync, existsSync } from 'node:fs';
          import { join } from 'node:path';

          const pages = ['dashboard.html', 'index.html', '404.html'];
          const assetRe = /(?:src|href)="(\/livewweb\/docs\/assets\/[^"]+|\/?assets\/[^"]+)"/g;
          const missing = [];

          for (const page of pages) {
            const file = join('dist', page);
            if (!existsSync(file)) continue;
            const html = readFileSync(file, 'utf8');
            if (!html.includes('/livewweb/docs/assets/')) {
              missing.push(`${page} -> missing /livewweb/docs/assets/ base paths`);
              continue;
            }
            for (const match of html.matchAll(assetRe)) {
              let rel = match[1].replace(/^\/livewweb\/docs\//, '').replace(/^\//, '');
              if (!existsSync(join('dist', rel))) missing.push(`${page} -> ${rel}`);
            }
          }

          if (missing.length) {
            console.error('Missing referenced assets:\n' + missing.join('\n'));
            process.exit(1);
          }
          console.log('Asset integrity check passed');
          NODE

      - name: Publish built site to docs/
        run: |
          cp -r docs/api /tmp/docs-api-backup
          rm -rf docs
          cp -r dist docs
          cp -r /tmp/docs-api-backup docs/api
          rm -rf /tmp/docs-api-backup
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs
          git diff --staged --quiet && echo "docs unchanged" && exit 0
          git commit -m "deploy: GitHub Pages static build [skip ci]"
          git push

      # Serve full repo (index.html redirects + docs/ production build).
      # Canonical dashboard: /livewweb/docs/dashboard.html
      # Do NOT use actions/deploy-pages here — artifact deploy serves dist at
      # /livewweb/ without /docs/, breaking /livewweb/docs/assets/ references.
      - name: Point GitHub Pages at repo root
        run: |
          code=$(curl -s -o /tmp/pages.json -w "%{http_code}" \
            -X PUT \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ github.repository }}/pages" \
            -d '{"build_type":"legacy","source":{"branch":"main","path":"/"}}')
          echo "Pages configure HTTP $code"
          cat /tmp/pages.json